import { UserObjectMother } from '../../object-mother/user-object-mother';

describe('Auth - Login', () => {
  it('Should ', () => {
    const user = UserObjectMother();
    console.log(user);
    expect(user).toBeDefined();
  });
});

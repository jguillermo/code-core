{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "coverage": "lerna run coverage --stream"

  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "lerna": "^8.1.2",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.3"
  }
}
